<% if can_modify? @user %>
  <%= cell(:order_form).(:new) %>
<% end %>

<table>
  <thead id = "order_forms_head">
    <%= cell(:order_form).(:head) %>
  </thead>

  <tbody id = "order_forms">
    <%= cell(:order_form).(:enum) %>
  </tbody>
</table>