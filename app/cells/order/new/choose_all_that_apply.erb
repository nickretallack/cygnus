<% @options.each_with_index do |option, index| %>
  <div class = "padded">
    <%= check_box_tag "content[#{@question}][#{index}]", option %>
    <%= label "content[#{@question}]", index, option %>
  </div>
<% end %>