<% enum_for @user.pms do |pm| %>
  <%= hidable pm.subject.blank?? "Conversation with #{pm.pm_author == @user? pm.recipient.name : pm.pm_author.name}" : pm.subject do %>
    <%= cell(:pm, pm).(:show) %>
    <%= cell(:pm, pm).(:new) %>
  <% end %>
<% end %>