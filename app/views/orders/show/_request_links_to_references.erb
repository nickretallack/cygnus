<% content = Array(content).reject{ |a| a.blank? } %>
<%if content.present?%>
	<% content.each do |link| %>
	  <blockquote class = "link"><%= auto_link link %></blockquote>
	<% end %>
<% else %>
	(No Response)
<% end %>