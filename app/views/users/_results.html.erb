<% @users.each do |user| %>
  <div class = "row">
    <hr />
    <div class = "row">
      <div class = "col s12">
        <div class = "fl thumb-large">
          <%= link_to image_tag(asset_path(:thumb, image_for(user: user))), user_path(user) %>
        </div>
        <div class = "fl ">
          <h4 class = "truncate"><%= link_to user.name, user_path(user) %></h4>
          <h4><%= statuses user, verbosity: :condensed %></h4>
          <% if user.galleries.first %>
            <span><%= link_to "Gallery", gallery_path(user) %><%= link_to "(ies)", pools_path(user) %></span>
          <% end %>
        </div>
      </div>
    </div>
    <%if !user.price.blank?%>
      <%=user.price%><br />
    <% end %>
    Tags: <%= user.tags || "(None)"%>
  </div>
<% end %>
