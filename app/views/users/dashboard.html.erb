<% provide(:header, link_to(@user.name, @user)+"'s Dashboard") %>

<% if at_least :admin %>
  <%= cell(:message).(:new_announcement) %>
<% end %>

<%= form_for @user, method: :patch, url: user_path(@user) do |f| %>
<% end %>