<% provide(:header, 'Sign up') %>

<div class="row" id="detach-point">
  <div class="col-md-6 col-md-offset-3">
  	<div id="form">
	    <%= form_for @user, multipart: true do |f| %>
	      <%= render 'fields', f: f %>
	      <%= f.submit "Create my account", class: "btn btn-primary spaced", id: "save-button" %>
	    <% end %>
	</div>
  </div>
</div>

<div id="attach-point" />
