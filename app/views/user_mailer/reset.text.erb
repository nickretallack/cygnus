To reset your <%= CONFIG[:name] %> password click the link below:

<%= reset_url(@user, @user.reset_token) %>

This link will expire in <%= CONFIG[:password_reset_shelf_life]/3600 %> hours.

Your password will remain as it is until you click the link above.
If you did not request your password to be reset, you may ignore this email.
