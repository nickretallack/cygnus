<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:header)? "Bleatr - "+yield(:header) : "Bleatr" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Bleatr" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag controller_name+"."+params[:action] %>
    <%= javascript_include_tag :modernizr %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <h1><%= yield :header %></h1>
      <%= render 'layouts/messages' %>
      <%= yield %>
    </main>
    <footer>
      <p>&copy;2015 Lunar Gryphon LLC
    </footer>
  </body>
</html>
