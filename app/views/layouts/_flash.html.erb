<% unless notice.blank? %>
  <p id="notice"><%= notice %></p>
<% end %>

<% if flash.any? %>
  <% flash.each do |type, message| %>
    <% if message.is_a? String %>
      <%= content_tag :div, id: "flash", class:"#{type}" do %>
        <%= format_flash message, type %>
      <% end %>
    <% end %>
  <% end %>
<% end %>

<% if flash[:errors] %>
  <div class="errors">
    <% flash[:errors].each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </div>
<% end %>