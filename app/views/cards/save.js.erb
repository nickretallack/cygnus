$("#"+'<%=j params[:id] %>').replaceWith('<%=j cell(:card, @card).(:show, user: @user) %>');
var card = $("#"+'<%= @card.id %>');
card.find(".button-submit").each(function(index, element){ new ButtonSubmit($(element)); });
switchToWorklistMode("edit");
var saveButton = $(card.find("#save"))
saveButton.css("color", "green");
setTimeout(function(){
  saveButton.css("color", "black");
}, 2000);