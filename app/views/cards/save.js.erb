$("#"+'<%=j params[:id] %>').replaceWith('<%=j cell(:card, @card).(:show, user: @user) %>');
var card = $("#"+'<%= @card.id %>');
initButtonAreas($(card.find(".button-submit")));
switchToWorklistMode("edit");
var saveButton = $(card.find("[title = Save]"))
saveButton.css("color", "green");
setTimeout(function(){
  saveButton.css("color", "black");
}, 2000);