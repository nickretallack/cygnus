var submissions = $("#submissions");
if(submissions.exists()){
  submissions.find("#" + <%= @submission.id %>).replaceWith("<%=j render "submissions/summary/edit", model: @submission %>");
  initialize();
}else{
  $("#submission").replaceWith("<%=j render "form" %>");
  initialize();
}