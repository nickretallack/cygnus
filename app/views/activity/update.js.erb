$("#unread-messages").html("<%=j render "activity/unread_messages" %>");
$("#older-messages").html("<%=j render "activity/older_messages" %>");
$("#unread-messages-count").text("<%= @user.unread_messages.length %>");
initialize();
$(window).trigger("resize.Hidable");